# ── Banco de dados ────────────────────────────────────────────────────────────
# Formato: postgresql://USUARIO:SENHA@HOST:PORTA/NOME_DO_BANCO
# Em produção (Railway): cole a URL gerada automaticamente pela plataforma
DATABASE_URL=postgresql://cognora:cognora@localhost:5432/cognora

# ── Segurança JWT ─────────────────────────────────────────────────────────────
# Gere um valor seguro com: python -c "import secrets; print(secrets.token_hex(32))"
# NUNCA use o valor padrão abaixo em produção
SECRET_KEY=troque-por-uma-chave-segura-gerada-aleatoriamente

# ── API de IA (OpenRouter) ────────────────────────────────────────────────────
# Obtenha em: https://openrouter.ai/keys
OPENROUTER_API_KEY=sk-or-v1-SUBSTITUA_PELA_SUA_CHAVE

# ── Stripe (pagamentos) ───────────────────────────────────────────────────────
# Chaves em: https://dashboard.stripe.com/apikeys
STRIPE_SECRET_KEY=sk_live_SUBSTITUA
# Crie o webhook em: Stripe Dashboard > Developers > Webhooks > Add endpoint
# URL: https://cognora.onrender.com/api/subscriptions/webhook
# Eventos: checkout.session.completed, customer.subscription.updated, customer.subscription.deleted
STRIPE_WEBHOOK_SECRET=whsec_SUBSTITUA
# IDs dos preços em: Stripe Dashboard > Products
STRIPE_PRICE_ID_PRO=price_SUBSTITUA_PRO
STRIPE_PRICE_ID_UNLIMITED=price_SUBSTITUA_UNLIMITED

# ── Email transacional (recuperação de senha) ─────────────────────────────────
# Obtenha em: https://resend.com/api-keys
RESEND_API_KEY=re_SUBSTITUA_PELA_SUA_CHAVE

# ── Google OAuth ──────────────────────────────────────────────────────────────
# Crie em: https://console.cloud.google.com → Credenciais → ID do cliente OAuth 2.0
GOOGLE_CLIENT_ID=SUBSTITUA_PELO_SEU_CLIENT_ID.apps.googleusercontent.com

# ── URL do frontend (para links no email) ────────────────────────────────────
APP_URL=http://localhost:5173

# ── CORS ─────────────────────────────────────────────────────────────────────
# Em desenvolvimento: deixe em branco ou use * (qualquer origem)
# Em produção: liste as origens permitidas separadas por vírgula
# Exemplo: ALLOWED_ORIGINS=https://cognora.vercel.app,https://www.cognora.com.br
ALLOWED_ORIGINS=*
